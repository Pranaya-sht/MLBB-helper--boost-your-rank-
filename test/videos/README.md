# Test replay videos

Drop `.mp4` files here for local Replay Timeline testing.

The Streamlit **Replay Timeline** tab can load videos from this folder (up to **200 MB** each via `.streamlit/config.toml`).

Example:

```text
test/videos/
  my_match.mp4
  ranked_game_01.mp4
```
